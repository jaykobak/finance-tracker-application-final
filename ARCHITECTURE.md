# 🏗️ Architecture Diagram - Finance Tracker

```
┌─────────────────────────────────────────────────────────────────────┐
│                           BROWSER (Client)                          │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    REACT FRONTEND                           │  │
│  │                    (Port 5173)                              │  │
│  │                                                             │  │
│  │  Components:                                                │  │
│  │  • Login/Signup Forms                                       │  │
│  │  • Transaction List                                         │  │
│  │  • Dashboard                                                │  │
│  │                                                             │  │
│  │  State Management:                                          │  │
│  │  • src/lib/auth.tsx  ─────► User authentication           │  │
│  │  • src/lib/store.ts  ─────► Transaction data              │  │
│  │  • src/lib/api.ts    ─────► API communication             │  │
│  │                                                             │  │
│  └─────────────────────┬───────────────────────────────────────┘  │
│                        │                                           │
│                        │ HTTP Requests (Fetch API)                 │
│                        │ + JWT Token in Headers                    │
│                        │                                           │
└────────────────────────┼───────────────────────────────────────────┘
                         │
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       NODE.JS SERVER                                │
│                       (Port 5000)                                   │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │                    EXPRESS APP                             │   │
│  │                                                            │   │
│  │  Middleware Stack:                                         │   │
│  │  ┌──────────────────────────────────────────────────┐    │   │
│  │  │  1. helmet() ────────► Security headers          │    │   │
│  │  │  2. cors() ──────────► Allow cross-origin        │    │   │
│  │  │  3. express.json() ──► Parse JSON body           │    │   │
│  │  └──────────────────────────────────────────────────┘    │   │
│  │                                                            │   │
│  │  Routes:                                                   │   │
│  │  ┌──────────────────────────────────────────────────┐    │   │
│  │  │  /api/auth/signup                                │    │   │
│  │  │  /api/auth/login                                 │    │   │
│  │  │  /api/auth/me ───────────► Auth Middleware      │    │   │
│  │  │                                                   │    │   │
│  │  │  /api/transactions ───────► Auth Middleware      │    │   │
│  │  │  /api/transactions/:id ───► Auth Middleware      │    │   │
│  │  │  /api/transactions/summary ► Auth Middleware     │    │   │
│  │  └──────────────────────────────────────────────────┘    │   │
│  │                                                            │   │
│  │  Controllers:                                              │   │
│  │  ┌──────────────────────────────────────────────────┐    │   │
│  │  │  authController.js                                │    │   │
│  │  │  • signup()  ─────► Create user + JWT            │    │   │
│  │  │  • login()   ─────► Verify + JWT                 │    │   │
│  │  │  • getProfile() ──► Get user info                │    │   │
│  │  │                                                   │    │   │
│  │  │  transactionController.js                         │    │   │
│  │  │  • getTransactions() ─► Fetch all                │    │   │
│  │  │  • createTransaction() ► Add new                 │    │   │
│  │  │  • deleteTransaction() ► Remove                  │    │   │
│  │  │  • getSummary() ──────► Calculate totals         │    │   │
│  │  └──────────────────────────────────────────────────┘    │   │
│  │                                                            │   │
│  └────────────────────┬───────────────────────────────────────┘   │
│                       │                                            │
│                       │ SQL Queries                                │
│                       │ (Parameterized)                            │
│                       │                                            │
└───────────────────────┼────────────────────────────────────────────┘
                        │
                        │
                        ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    POSTGRESQL DATABASE                              │
│                    (Port 5432)                                      │
│                                                                     │
│  Database: finance_tracker                                          │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  TABLE: users                                                │  │
│  │  ┌────┬──────────┬──────────────────┬─────────────────────┐ │  │
│  │  │ id │   name   │      email       │      password       │ │  │
│  │  ├────┼──────────┼──────────────────┼─────────────────────┤ │  │
│  │  │ 1  │ John Doe │ john@example.com │ $2a$10$hashed...   │ │  │
│  │  │ 2  │ Jane     │ jane@example.com │ $2a$10$hashed...   │ │  │
│  │  └────┴──────────┴──────────────────┴─────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │  TABLE: transactions                                         │  │
│  │  ┌────┬────────┬────────┬────────┬─────────────┬─────────┐ │  │
│  │  │ id │user_id │  type  │ amount │ description │ category│ │  │
│  │  ├────┼────────┼────────┼────────┼─────────────┼─────────┤ │  │
│  │  │ 1  │   1    │ income │ 5000   │ Salary      │ Work    │ │  │
│  │  │ 2  │   1    │ expense│ 1200   │ Rent        │ Housing │ │  │
│  │  │ 3  │   2    │ income │ 3000   │ Freelance   │ Work    │ │  │
│  │  └────┴────────┴────────┴────────┴─────────────┴─────────┘ │  │
│  │                                                              │  │
│  │  Indexes:                                                    │  │
│  │  • idx_transactions_user_id  (fast user lookups)            │  │
│  │  • idx_transactions_date     (fast date filtering)          │  │
│  │  • idx_transactions_type     (fast type filtering)          │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  Foreign Keys:                                                      │
│  transactions.user_id ────► users.id                               │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘


═════════════════════════════════════════════════════════════════════
                        REQUEST FLOW EXAMPLE
═════════════════════════════════════════════════════════════════════

🔄 User Logs In:
─────────────────

  1. User enters email + password in React form
              ↓
  2. Frontend: authAPI.login(email, password)
              ↓
  3. HTTP POST → http://localhost:5000/api/auth/login
              ↓
  4. Express routes to authController.login()
              ↓
  5. Controller queries: SELECT * FROM users WHERE email = ?
              ↓
  6. PostgreSQL returns user record
              ↓
  7. bcrypt.compare(password, hashedPassword)
              ↓
  8. jwt.sign({ id: user.id }, SECRET)
              ↓
  9. Response: { success: true, token: "eyJ...", user: {...} }
              ↓
  10. Frontend stores token in localStorage
              ↓
  11. User is now logged in!


🔄 User Adds Transaction:
──────────────────────────

  1. User fills transaction form and clicks "Add"
              ↓
  2. Frontend: transactionsAPI.create(transaction)
              ↓
  3. HTTP POST → http://localhost:5000/api/transactions
     Headers: Authorization: Bearer eyJhbGc...
              ↓
  4. Express Middleware: authenticateToken()
              ↓
  5. JWT verified → Extract user.id
              ↓
  6. Controller: createTransaction()
              ↓
  7. SQL: INSERT INTO transactions (user_id, type, amount, ...)
              ↓
  8. PostgreSQL saves and returns new record
              ↓
  9. Response: { success: true, transaction: {...} }
              ↓``
  10. Frontend updates UI with new transaction
              ↓
  11. Transaction appears in list!


🔄 User Views Dashboard:
─────────────────────────

  1. Component mounts → useTransactions() hook
              ↓
  2. Frontend: transactionsAPI.getAll()
              ↓
  3. HTTP GET → http://localhost:5000/api/transactions
     Headers: Authorization: Bearer eyJhbGc...
              ↓
  4. Middleware verifies token → user.id
              ↓
  5. Controller: getTransactions()
              ↓
  6. SQL: SELECT * FROM transactions WHERE user_id = ?
              ↓
  7. PostgreSQL returns all user's transactions
              ↓
  8. Response: { success: true, transactions: [...] }
              ↓
  9. Frontend calculates summary and renders
              ↓
  10. User sees their complete financial data!


═════════════════════════════════════════════════════════════════════
                          SECURITY LAYERS
═════════════════════════════════════════════════════════════════════

Layer 1: Password Security
  • Passwords hashed with bcrypt (10 rounds)
  • Never stored or transmitted in plain text
  • One-way encryption (can't be reversed)

Layer 2: JWT Authentication
  • Token contains encrypted user ID
  • Token signed with secret key
  • Expires after 7 days
  • Verified on every protected request

Layer 3: User Isolation
  • Each user sees only THEIR data
  • SQL queries filter by user_id
  • Foreign key constraints enforce relationships

Layer 4: Input Validation
  • Request data validated
  • SQL injection prevented (parameterized queries)
  • Error handling prevents data leaks

Layer 5: HTTP Security
  • helmet.js adds security headers
  • CORS configured for specific origin
  • HTTPS ready for production


═════════════════════════════════════════════════════════════════════
                        DATA FLOW SUMMARY
═════════════════════════════════════════════════════════════════════

┌──────────┐     HTTP      ┌────────┐     SQL      ┌──────────┐
│          │ ───────────► │        │ ──────────► │          │
│ Frontend │              │ Server │              │ Database │
│  (React) │ ◄─────────── │ (Node) │ ◄────────── │  (PSQL)  │
└──────────┘    JSON       └────────┘    Rows      └──────────┘

   State          ↕            ↕            ↕
   Updates     Routes     Controllers   Tables
                +            +             +
              Token      Middleware    Indexes


═════════════════════════════════════════════════════════════════════

This architecture provides:
✅ Security (authentication, authorization, encryption)
✅ Scalability (can handle growth)
✅ Maintainability (organized, clean code)
✅ Reliability (error handling, validation)
✅ Performance (database indexes, efficient queries)

```
